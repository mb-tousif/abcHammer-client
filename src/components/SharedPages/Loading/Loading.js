import React from 'react';

const Loading = () => {
    return (
      <div className="w-full flex justify-center">
        <button class="btn loading mt-16">loading</button>
      </div>
    );
};

export default Loading;